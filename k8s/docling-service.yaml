apiVersion: v1
kind: Service
metadata:
  name: docling-ocr-service
  namespace: actas-colegio
  labels:
    app: docling-ocr
spec:
  selector:
    app: docling-ocr
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
